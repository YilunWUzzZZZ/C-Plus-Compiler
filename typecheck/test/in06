/* 
 * Test: all
 */

    int getpid_t(int t);
    int logMessage(string x);
    int logMessageInt(string x, int y) ;    
    int getpid();
    int isChild(int a);


int tmp;
int i = 2;
int j = 3;
int k = 4.0;
double f = 3.3;
int y;
string state_var;

bool bo, bo1, bo2, bo3, bo4;

        
int open(string file4,int flags4, int mode4)  {
	if(!(i<6) && i<=j || j >=k)
                if ((i < j) || (j > k)) {
                        f = (23 * (-i)/j) - f  - (45 % f) ;
                }
                else {
                        j = i & (j | k) ^ i & (~j);
                }
        else
                k = k << (j >> i) ;

        if(!i)
                if (i+j) {
                        f = (23 * (-i)/j) - f  - (45 % j) ;
                        bo = ((bo3 < bo1) || (bo > bo2)) ;
                }

};

        
